# PRECOMPUTE / heavy lane (local-only, NEVER deployed), the .venv-precompute that trains the two learned models.
# Pinned to the verified CPU install (Python 3.12/3.13). The default lane (data-pipeline/requirements.txt) stays
# numpy-only so CI + the replay pipeline never need torch.
numpy>=1.26
torch==2.6.0
onnx==1.17.0
