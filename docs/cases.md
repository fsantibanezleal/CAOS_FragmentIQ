# Cases

The CATEGORY taxonomy + the coverage matrix live in **[cases/README.md](cases/README.md)** (per-case deep pages
are not written yet). Each case carries a category, params, an expected band, and a
real|synthetic flag; the App shows one case, Experiments/Benchmark summarize across categories.
